import "test_import_imported.proto";

message ContainsImported {
    optional ImportedMessage imported_message = 1;
    optional ImportedEnum imported_enum = 2;
    optional ImportedMessage.Child imported_child = 3;
}
